---
import { getEntry } from 'astro:content';
import PublicPage from '../components/PublicPage.astro';

const page = await getEntry('public-pages', 'pricing');

if (!page) {
  throw new Error('Missing pricing page content.');
}
---

<PublicPage page={page} />
