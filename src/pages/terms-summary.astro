---
import { getEntry } from 'astro:content';
import PublicPage from '../components/PublicPage.astro';

const page = await getEntry('public-pages', 'terms-summary');

if (!page) {
  throw new Error('Missing terms-summary page content.');
}
---

<PublicPage page={page} />
